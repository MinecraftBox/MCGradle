allprojects { project ->
    if (project == project.rootProject)
        return

    afterEvaluate {
        project.tasks.jar {
            destinationDir = project.file("${recompDir}")
            archiveName = "${jarName}"
        }
    }
}